@model IEnumerable<cbapp.Models.Songs>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.length)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Project)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tracklist_number)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.length)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Project.artist)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tracklist_number)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.song_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.song_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.song_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
